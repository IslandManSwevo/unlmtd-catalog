# Copy this to .env for local development (never commit .env)

# PostgreSQL connection string (auto-set by Railway when you add a Postgres plugin)
DATABASE_URL=postgresql://user:password@localhost:5432/unlmtd

# Your admin password — set this in Railway environment variables
ADMIN_PASSWORD=your_secure_password_here
